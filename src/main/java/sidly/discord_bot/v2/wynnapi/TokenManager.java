package sidly.discord_bot.v2.wynnapi;

public class TokenManager {
    // TODO should support any number of tokens and handle private / public differently
    //      handle ratelimit with what type of limit and send message
    //      handle per server config tokens as well as global bot tokens
    //      handle invalid tokens to not count if token count is neccassary ie for global token looping
}
